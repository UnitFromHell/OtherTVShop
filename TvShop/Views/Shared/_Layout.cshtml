
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - TVShop</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <!-- ШРИФТЫ -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
    <!-- Css -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/contact.css" />
    <link rel="stylesheet" href="~/css/home.css" />
    <link rel="stylesheet" href="~/css/privacy.css" />
    <link rel="stylesheet" href="~/css/product.css" />
    <link rel="stylesheet" href="~/css/catalog.css" />
    <link rel="stylesheet" href="~/css/layout.css" asp-append-version="true" />
   
</head>

<body>
    <header class="shadow-sm">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <a asp-controller="Home" asp-action="Index" class="logo-box">
                <div class="logo-circle">TV</div>
                <span class="text-2xl font-semibold text-gray-800 tracking-tight">TVShop</span>
            </a>

            <nav class="hidden md:flex items-center gap-3">
                <a asp-controller="Home" asp-action="Index"
                   class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString()=="Home" && ViewContext.RouteData.Values["Action"]?.ToString()=="Index"?"active":"")">
                    Главная
                </a>
                <a asp-controller="Product" asp-action="Index"
                   class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString()=="Product"?"active":"")">
                    Товары
                </a>
                <a asp-controller="Home" asp-action="Privacy"
                   class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString()=="Privacy"?"active":"")">
                    Политика
                </a>
                <a asp-controller="Contact" asp-action="Index"
                   class="nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString()=="Contact"?"active":"")">
                    Контакты
                </a>
            </nav>

            <div class="flex items-center gap-4">
                <a asp-controller="Cart" asp-action="Index" class="cart-modern relative">
                    <i class="fas fa-shopping-bag cart-icon"></i>
                    <span id="cartCount" class="cart-count hidden">0</span>
                </a>

@if (User.Identity?.IsAuthenticated ?? false)
{
    <div class="user-info">
        <span class="user-name">@User.Identity.Name</span>
        <form asp-controller="Account" asp-action="Logout" method="post" class="inline">
            <button type="submit" class="logout-btn text-sm">
                Выйти
            </button>
        </form>
    </div>
}
else
{
    <div class="flex items-center gap-3">
        <a asp-controller="Account" asp-action="Login" class="auth-link login text-sm">
            Войти
        </a>
        <a asp-controller="Account" asp-action="Register" class="auth-link register text-sm">
            Регистрация
        </a>
    </div>
}

                <button id="mobileMenuButton" class="md:hidden text-gray-700 text-2xl p-2 rounded-lg hover:bg-gray-100 transition-colors">
                    <i class="fas fa-bars"></i>
                </button>
            </div>
        </div>

<div id="mobileMenu" class="hidden bg-white border-t shadow-inner md:hidden">
    <nav class="flex flex-col p-4 space-y-2">
        <a asp-controller="Home" asp-action="Index" 
           class="mobile-nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString()=="Home" && ViewContext.RouteData.Values["Action"]?.ToString()=="Index"?"active":"")">
            Главная
        </a>
        <a asp-controller="Product" asp-action="Index" 
           class="mobile-nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString()=="Product"?"active":"")">
            Товары
        </a>
        <a asp-controller="Home" asp-action="Privacy" 
           class="mobile-nav-link @(ViewContext.RouteData.Values["Action"]?.ToString()=="Privacy"?"active":"")">
            Политика
        </a>
        <a asp-controller="Contact" asp-action="Index" 
           class="mobile-nav-link @(ViewContext.RouteData.Values["Controller"]?.ToString()=="Contact"?"active":"")">
            Контакты
        </a>

        @if (!(User.Identity?.IsAuthenticated ?? false))
        {
            <div class="border-t pt-4 mt-2 space-y-2">
                <a asp-controller="Account" asp-action="Login" class="mobile-nav-link">
                    Войти
                </a>
                <a asp-controller="Account" asp-action="Register" class="mobile-nav-link">
                    Регистрация
                </a>
            </div>
        }
    </nav>
</div>
    </header>

    <main>
        @RenderBody()
    </main>

    <footer class="footer text-white pt-16 pb-8">
        <div class="container mx-auto px-4">
            <div class="footer-grid">
                <div>
                    <div class="footer-logo">
                        <div class="footer-logo-circle">TV</div>
                        <span class="text-2xl font-bold">TVShop</span>
                    </div>
                    <p class="text-gray-300 mb-6 leading-relaxed">
                        Ведущий магазин телевизоров премиум-класса. Мы предлагаем только лучшее — от элегантных OLED-панелей до умных QLED-моделей с искусственным интеллектом.
                    </p>
                    <div class="social-links">
                        <a href="#" class="social-link">
                            <i class="fab fa-vk"></i>
                        </a>
                        <a href="#" class="social-link">
                            <i class="fab fa-telegram"></i>
                        </a>
                        <a href="#" class="social-link">
                            <i class="fab fa-youtube"></i>
                        </a>
                        <a href="#" class="social-link">
                            <i class="fab fa-whatsapp"></i>
                        </a>
                    </div>
                </div>

                <div>
                    <h3 class="footer-heading">Навигация</h3>
                    <div class="space-y-2">
                        <a asp-controller="Home" asp-action="Index" class="footer-link">
                            <i class="fas fa-home"></i>
                            Главная
                        </a>
                        <a asp-controller="Product" asp-action="Index" class="footer-link">
                            <i class="fas fa-tv"></i>
                            Каталог
                        </a>
                        <a asp-controller="Home" asp-action="Privacy" class="footer-link">
                            <i class="fas fa-shield-alt"></i>
                            Политика
                        </a>
                        <a asp-controller="Contact" asp-action="Index" class="footer-link">
                            <i class="fas fa-phone"></i>
                            Контакты
                        </a>
                    </div>
                </div>

                <div>
                    <h3 class="footer-heading">Помощь</h3>
                    <div class="space-y-2">
                        <a href="#" class="footer-link">
                            <i class="fas fa-shipping-fast"></i>
                            Доставка и оплата
                        </a>
                        <a href="#" class="footer-link">
                            <i class="fas fa-wrench"></i>
                            Установка и настройка
                        </a>
                        <a href="#" class="footer-link">
                            <i class="fas fa-file-contract"></i>
                            Гарантия
                        </a>
                        <a href="#" class="footer-link">
                            <i class="fas fa-question-circle"></i>
                            FAQ
                        </a>
                    </div>
                </div>

                <div>
                    <h3 class="footer-heading">Новости и акции</h3>
                    <p class="text-gray-300 mb-4">Подпишитесь на рассылку и получите скидку 5% на первую покупку!</p>
                    <form class="space-y-3">
                        <input type="email" placeholder="Ваш email" class="newsletter-input" required>
                        <button type="submit" class="newsletter-btn">
                            <i class="fas fa-paper-plane mr-2"></i>
                            Подписаться
                        </button>
                    </form>
                </div>
            </div>
            <div class="footer-bottom">
                <div class="payment-methods">
                    <div class="payment-method">VISA</div>
                    <div class="payment-method">MC</div>
                    <div class="payment-method">MIR</div>
                    <div class="payment-method">QIWI</div>
                    <div class="payment-method">SBP</div>
                </div>

                <div class="flex flex-col md:flex-row justify-between items-center gap-4 text-gray-400 text-sm">
                    <div class="text-center md:text-left">
                        <p>&copy; 2025 TVShop. Все права защищены. Качество, которое видно 👀</p>
                    </div>
                    <div class="flex items-center gap-6">
                        <div class="flex items-center gap-2">
                            <i class="fas fa-phone text-blue-400"></i>
                            <span>8 (800) 555-35-35</span>
                        </div>
                        <div class="flex items-center gap-2">
                            <i class="fas fa-envelope text-purple-400"></i>
                            <span>info@tvshop.ru</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    @if (TempData["SuccessMessage"] != null)
    {
        <div id="successNotification" class="notification success">
            @TempData["SuccessMessage"]
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div id="errorNotification" class="notification error">
            @Html.Raw(TempData["ErrorMessage"])
        </div>
    }


    <script src="~/js/cart.js" asp-append-version="true"></script>
    <script src="~/js/layout.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>